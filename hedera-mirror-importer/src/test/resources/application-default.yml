hedera:
  mirror:
    importer:
      db:
        name: mirror_node_test
      downloader:
        bucketName: test
        cloudProvider: LOCAL
      parser:
        balance:
          enabled: false
        exclude:
          - entity: 0.0.999
      network: TESTNET
spring:
  task:
    scheduling:
      enabled: false
