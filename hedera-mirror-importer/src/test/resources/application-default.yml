hedera:
  mirror:
    db:
      name: mirror_node_test
    downloader:
      bucketName: test
      cloudProvider: LOCAL
    network: TESTNET
    parser:
      balance:
        enabled: false
spring:
  task:
    scheduling:
      enabled: false
