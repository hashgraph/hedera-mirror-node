hedera:
  mirror:
    db:
      name: mirror_node_test
    downloader:
      bucketName: test
      cloudProvider: LOCAL
    importer:
      parser:
        exclude:
          - entity: 0.0.999
    network: TESTNET
    parser:
      balance:
        enabled: false
spring:
  cloud:
    kubernetes:
      enabled: false
  task:
    scheduling:
      enabled: false
