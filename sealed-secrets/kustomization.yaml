apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: sealed-secrets
  namespace: flux-system
spec:
  interval: 1m
  path: ./sealed-secrets
  prune: true
  sourceRef:
    kind: GitRepository
    name: hedera-mirror-node
  validation: client
