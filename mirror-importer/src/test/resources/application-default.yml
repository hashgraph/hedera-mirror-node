hedera:
  mirror:
    downloader:
      bucketName: test
      cloudProvider: LOCAL
    network: TESTNET
    parser:
      balance:
        enabled: false
spring:
  task:
    scheduling:
      enabled: false
