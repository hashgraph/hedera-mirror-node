# We upload three separate reports: js, java and go
codecov:
    notify:
        after_n_builds: 3
comment:
    after_n_builds: 3
coverage:
    status:
        project:
            default:
                threshold: 1.0
