apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: cluster-critical
value: 10000
globalDefault: false
description: "Used for critical priority workloads that are used across the cluster"
