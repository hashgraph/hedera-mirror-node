apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mirror
  namespace: mainnet
spec:
  chart:
    spec:
      chart: hedera-mirror
      sourceRef:
        kind: HelmRepository
        name: hedera-mirror-node
      valuesFiles:
        - values.yaml
        - values-prod.yaml
      version: '0.39.0'
  dependsOn:
    - name: mirror
      namespace: common
  install:
    crds: CreateReplace
  interval: 90s
  test:
    enable: false
    timeout: 5m
  timeout: 48h
  upgrade:
    cleanupOnFail: true
    crds: CreateReplace
    remediation:
      retries: 20
  valuesFrom:
    - kind: Secret
      name: mirror
  values:
    grpc:
      ingress:
        enabled: false
        middleware:
          enabled: false
      service:
        annotations:
          cloud.google.com/neg: '{"ingress": true, "exposed_ports": {"5600": {}, "8081": {}}}'
    importer:
      env:
        HEDERA_MIRROR_IMPORTER_NETWORK: "MAINNET"
        HEDERA_MIRROR_IMPORTER_START_DATE: "1970-01-01T00:00:00.000000000Z"
    monitor:
      env:
        HEDERA_MIRROR_MONITOR_NETWORK: "MAINNET"
        HEDERA_MIRROR_MONITOR_NODEVALIDATION_MAXATTEMPTS: "8"
        HEDERA_MIRROR_MONITOR_PUBLISH_SCENARIOS_PINGER_PROPERTIES_TOPICID: "0.0.797576"
        HEDERA_MIRROR_MONITOR_SUBSCRIBE_GRPC_HCS_TOPICID: "0.0.797576"
      ingress:
        enabled: false
      prometheusRules:
        MonitorPublishLatency:
          enabled: false
        MonitorSubscribeLatency:
          enabled: false
      service:
        annotations:
          cloud.google.com/neg: '{"ingress": true}'
    postgresql:
      enabled: false
    redis:
      enabled: false
    rest:
      ingress:
        enabled: false
        middleware:
          enabled: false
      service:
        annotations:
          cloud.google.com/neg: '{"ingress": true}'
    rosetta:
      env:
        HEDERA_MIRROR_ROSETTA_NETWORK: "mainnet"
      ingress:
        enabled: false
        middleware:
          enabled: false
      service:
        annotations:
          cloud.google.com/neg: '{"ingress": true}'
    web3:
      ingress:
        enabled: false
        middleware:
          enabled: false
      service:
        annotations:
          cloud.google.com/neg: '{"ingress": true}'
