apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: github-dispatch
  namespace: flux-system
spec:
  eventMetadata:
    cluster: mainnet-eu
    namespaces: common,mainnet,mainnet-citus
  eventSeverity: info
  eventSources:
    - kind: HelmRelease
      name: mirror
      namespace: mainnet
  inclusionList:
    - ".*.test.*succeeded.*"
  providerRef:
    name: github-dispatch
  summary: "Trigger mainnet-eu deployment"

