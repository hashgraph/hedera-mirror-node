# This file contains application defaults and shouldn't be modified. See docs/configuration.md for how to override.
hedera:
  mirror:
    api:
      includeHostInLink: false
      maxLimit: 1000
      log:
        level: debug
      port: 5551
      metrics:
        enabled: true
        config:
          authentication: true
          username: mirror_api_metrics
          password: mirror_api_metrics_pass
          uriPath: '/swagger'
    db:
      apiPassword: mirror_api_pass
      apiUsername: mirror_api
      host: 127.0.0.1
      name: mirror_node
      port: 5432
    shard: 0
