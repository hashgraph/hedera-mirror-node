apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: mirror
  namespace: dev
spec:
  chart:
    git: https://github.com/hashgraph/hedera-mirror-node.git
    path: charts/hedera-mirror
    ref: 3cdf238b7e66e950e2439ca2ae3ee269b6619dc4
  rollback:
    enable: true
    force: true
    timeout: 500
    wait: true
  timeout: 500
  wait: false
  values:
    grpc:
      image:
        tag: 0.15.3
    importer:
      config:
        hedera:
          mirror:
            importer:
              downloader:
                cloudProvider: GCP
              network: TESTNET
      image:
        tag: 0.15.3
    postgresql:
      enabled: false
    rest:
      image:
        tag: 0.15.3
  valuesFrom:
    - secretKeyRef:
        name: mirror

