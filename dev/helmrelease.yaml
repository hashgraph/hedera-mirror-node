apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: mirror
  namespace: dev
spec:
  chart:
    git: https://github.com/hashgraph/hedera-mirror-node.git
    path: charts/hedera-mirror
    ref: b8537a0e5fbc17098696c5f52ab5a6d1d3680b35
  rollback:
    enable: true
    force: true
    timeout: 500
    wait: true
  timeout: 500
  wait: false
  values:
    grpc:
      image:
        tag: 0.15.3
    importer:
      config:
        hedera:
          mirror:
            importer:
              downloader:
                cloudProvider: GCP
              network: TESTNET
      image:
        tag: 0.15.3
    postgresql:
      enabled: false
    rest:
      image:
        tag: 0.15.3
  valuesFrom:
    - secretKeyRef:
        name: mirror

